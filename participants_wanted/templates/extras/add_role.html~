<!DOCTYPE html>
<html>
    <head>
        <title>Extras</title>
    </head>

    <body>
        <h1>Add a Role</h1>
	{{production_name_url}}
        {% if not production_name_url %}
            <div style="color: red;">The specified production does not exist!</div>
        {% else %}
            <form id="role_form" method="post" action="/extras/production/{{ production_name_url }}/add_role/">

                {% csrf_token %}
                {% for hidden in form.hidden_fields %}
                    {{ hidden }}
                {% endfor %}

                {% for field in form.visible_fields %}
                    {{ field.errors }}
                    {{ field.help_text}}
                    {{ field }}
                {% endfor %}

                <input type="submit" name="submit" value="Create Role" />
            </form>
        {% endif %}

    </body>

</html>
